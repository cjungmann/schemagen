#!/usr/bin/env python

"""Generate MySQL Procedures from a Table for Schema Framework Application.

"""

import sys

import sgdb
from sgscripts import SGScripter

SCRIPT_DELIMITER = "$$"

# Global dictionary with some default values that stores command line options.
arg_dict = {
    "host_name": "192.168.0.20",
    "user_name": "chuck",
    "user_password": None,
    "database_name": None,
    "table_name": None,
    "list": None,
    "action": None
}

# Dictionary to connect command line flags to arg_dict keys
label_dict = {
    "H": "host_name",
    "u": "user_name",
    "p": "user_password",
    "d": "database_name",
    "t": "table_name",
    "l": "display list",
    "a": "action"
}

actions_dict = {
    "h": "show_usage",
}

options_dict = {
    "h": "show usage (help)",
    "u": "mysql user name",
    "p": "mysql user password",
    "H": "mysql host name",
    "d": "mysql database name",
    "t": "mysql table name",
    "a": "schemagen action"
}


def show_usage():
    """Auto-generate a usage display from argument-handling dictionaries."""
    print("The available options are:")
    for key, value in options_dict.items():
        print(f"{key}:  {value}")

def key_from_value(generic_dict, value):
    """ Returns first dictionary item whose value matches 'value'
    Args:
       dict (dictionary): Dictionary to be searched
       value (string):    Value to be matched to a dictionary element

    Returns:
       key value for matching item, None if not found
       desired item, or None if not found
    """
    for item in generic_dict.items():
        if item[1] == value:
            return item[0]

    return None

def set_arg_dict(flag, value):
    """ Sets a dictionary value with command line options flags.
    Args:
       None

    Returns:
      None
    """
    if flag in label_dict:
        arg_dict[label_dict[flag]] = value

def process_cl_args():
    """ Read command line arguments to set global arg_dict.
    Args:
       None

    Returns:
      Boolean: true if successful, false otherwise
    """
    inarg = None
    for arg in sys.argv[1:]:
        if inarg is not None:
            set_arg_dict(inarg, arg)
            inarg = None
        elif len(arg) and arg[0] == '-':
            if arg[1] in label_dict:
                inarg = arg[1]
            else:
                print("Argument {} is not recognized.".format(arg))
                return False

    return True

def validate_cl_args():
    """ Identify missing or bad info and display args before running.

    This function isn't doing anything now, but it will when the
    more complicated options are implemented.

    Args:
       None

    Returns:
       True if arguments are valid, False otherwise
    """
    arg_values_set = True
    # for key, value in arg_dict.items():
    #     if value is None and key != "table_name":
    #         arg_values_set = False
    #         cl_option = key_from_value(label_dict, key)
    #         if cl_option:
    #             print("User must provide value for {} (use -{}).".format(key, cl_option))
    #         else:
    #             print("User must provide value for {} (ERROR finding option).".format(key))

    return arg_values_set

def show_cl_args():
    """List values of the global user-choices "arg_dict" variable.
    """
    for key, value in arg_dict.items():
        print("{}: {}".format(key, value))
        

def print_procedure_intro():
    """Print some simple introduction text to set off procedures in code."""
    print()
    print( "-- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- --" )

def process_connection(conn):
    """With a good connection, now execute the 'mission' set out in the parameters.

    Args:
       conn (object): An open MySQL/MariaDB connection

    Returns:
       None
    """
    db_name = arg_dict["database_name"]
    table_name = arg_dict["table_name"]

    if db_name is None:
        sgdb.show_database_names(conn)
    elif table_name is None:
        sgdb.show_table_names(conn, db_name)
    else:
        cdefs = sgdb.collect_table_columns(conn, db_name, table_name)

        print("DELIMITER " + SCRIPT_DELIMITER)

        scripter = SGScripter(delimiter=SCRIPT_DELIMITER)
        print_procedure_intro()
        scripter.print_proc_list(cdefs, table_name, "App_Person_List")
        print_procedure_intro()
        scripter.print_proc_add(cdefs, table_name, "App_Person_Add", "App_Person_List")
        print_procedure_intro()
        scripter.print_proc_read(cdefs, table_name, "App_Person_Read")
        print_procedure_intro()
        scripter.print_proc_update(cdefs, table_name, "App_Person_Update", "App_Person_List")
        print_procedure_intro()
        scripter.print_proc_delete(cdefs, table_name, "App_Person_Delete")

        scripter.get_autonumber_primary_key(cdefs)

        print()
        print("DELIMITER ;")


def main():
    """A main() function to satisfy the C-programmer in me."""
    if len(sys.argv) == 1:
        show_usage()
    else:
        if process_cl_args():

            show_cl_args();
            input("Press ENTER to continue.")

            if validate_cl_args():
                conn = sgdb.make_connection(arg_dict["host_name"],
                                            arg_dict["user_name"],
                                            arg_dict["user_password"])
                if conn:
                    process_connection(conn)
                    conn.close()
                else:
                    print("Failed to get a connection.")
            else:
                print("Can't proceed without missing arguments.")
        else:
            print("Failed to process the command line arguments.")

if __name__ == "__main__":
    main()
